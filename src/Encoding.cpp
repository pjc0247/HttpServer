#include "stdafx.h"
#include "Encoding.h"

using namespace std;

Encoding::Encoding(){
}
Encoding::~Encoding(){
}

string Encoding::encodeURL(const string &str){
	return "";
}
string Encoding::decodeURL(const string &str){
	return "";
}